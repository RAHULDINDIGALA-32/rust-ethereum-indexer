[package]
name = "ingestion"
version = "0.1.0"
edition = "2024"

[dependencies]

bigdecimal = {workspace = true}
rpc = { path = "../rpc" }
decoder = { path = "../decoder" }
storage = { path = "../storage" }

